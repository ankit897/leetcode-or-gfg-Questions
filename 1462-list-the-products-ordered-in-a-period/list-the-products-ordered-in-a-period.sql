# Write your MySQL query statement below
select product_name, sum(unit) as unit from Products as p join Orders as o on p.product_id = o.product_id where order_date like '2020-02%' group by o.product_id having unit>=100